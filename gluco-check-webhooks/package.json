{
  "name": "gluco-check-webhooks",
  "version": "0.0.1",
  "description": "Webhooks called by Google Assistant",
  "main": "build/src/index.js",
  "repository": "https://github.com/nielsmaerten/nightscout-assistant",
  "author": "Niels Maerten",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@types/jest": "^26.0.14",
    "@types/node": "^14.11.2",
    "firebase-functions-test": "^0.2.2",
    "gts": "^2.0.2",
    "jest": "^26.4.2",
    "rimraf": "^3.0.2",
    "shell-exec": "^1.0.2",
    "typescript": "^4.0.3"
  },
  "dependencies": {
    "@assistant/conversation": "^3.1.0",
    "firebase-admin": "^9.2.0",
    "firebase-functions": "^3.11.0",
    "gluco-check-common": "*",
    "gluco-check-core": "*"
  },
  "engines": {
    "node": ">=10"
  },
  "scripts": {
    "build": "rimraf build && tsc",
    "lint": "eslint {src,test}/**/*",
    "lint:fix": "eslint --fix {src,test}/**/*",
    "watch:test": "jest --watch",
    "watch:compile": "tsc --watch",
    "purge": "rimraf node_modules/",
    "test": "yarn build && jest && yarn lint",
    "serve": "echo !! Start 'watch:compile' in a separate process && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  }
}
